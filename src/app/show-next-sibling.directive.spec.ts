import { ShowNextSiblingDirective } from './show-next-sibling.directive';

describe('ShowNextSiblingDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowNextSiblingDirective();
    expect(directive).toBeTruthy();
  });
});
