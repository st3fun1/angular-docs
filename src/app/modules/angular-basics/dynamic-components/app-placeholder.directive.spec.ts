import { AppPlaceholderDirective } from './app-placeholder.directive';

describe('AppPlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
